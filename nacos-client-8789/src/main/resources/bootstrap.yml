spring:
  application:
    name: nacos-client-8789
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        # 指定注册中心地址
        server-addr: ${spring.cloud.nacos.server-addr}
      config:
        # 指定配置中心的地址
        server-addr: ${spring.cloud.nacos.server-addr}
        # 指定namespace
        namespace: 028f5307-771e-4f6f-af58-21921435fdfa
        # 指定配置组
        group: baizhi-springcloud
        # 指定拉取文件的后缀
        file-extension: yaml
  # 激活dev配置
  profiles:
    active: dev

